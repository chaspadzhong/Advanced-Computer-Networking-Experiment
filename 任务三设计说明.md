# 开发任务三设计说明

## 1. 任务目标

本任务在任务一的报文类型与任务二的延迟 ACK 基础上，实现两种基础丢包检测信号：发送端超时缩小窗口，以及接收端生成、发送端检测重复 ACK。本任务只检测丢包，不实现重传或 RACK。

## 2. 超时检测与窗口调整

`sender.cc` 在轮询超时时调用 `send_datagram( true )`，并把 `after_timeout=true` 传递给 `Controller::datagram_was_sent()`。控制器将此参数视为超时信号。

控制器新增 `window_size_`，初始值为 50。超时时，若窗口大于 1 就将其减半，并输出：

```text
time out, window size=<当前窗口值>
```

窗口最低为 1，避免连续超时将发送窗口缩减至 0。`window_size()` 返回该成员变量，因此发送端后续会按新的窗口限制未确认报文数量。

## 3. 重复 ACK 机制

接收端维护 `next_expected_sequence_number`（下一个期待的按序序号）和 `last_in_order_sequence_number`（最后一个按序序号）。收到期望序号时，接收端推进两个状态；收到乱序报文时，接收端在转换 ACK 前将当前报文的序列号替换为最后一个按序序号。

`transform_into_ack()` 会将该序列号写入 `ack_sequence_number`。因此，后续乱序报文会生成相同确认号的 ACK，即重复 ACK。任务二“每两个报文发送一个 ACK”的规则继续生效。

发送端的 `ack_received()` 使用 `last_ack_` 和 `has_last_ack_` 保存最近确认号。当前确认号与最近确认号相同时，输出：

```text
got same ack <序列号>
```

原任务二的 `num_acked:<序列号>` 输出继续保留，便于同时观察普通确认与重复确认。

## 4. 涉及文件

- `datagrump/controller.hh`：窗口与最近 ACK 状态。
- `datagrump/controller.cc`：超时缩窗及重复 ACK 检测。
- `datagrump/receiver.cc`：按序接收状态与重复 ACK 生成。

## 5. 运行与截图记录（待补充）

本次提交只完成代码和说明文档，未执行运行测试。可在后续实验中补充：

1. 包含 `time out, window size=...` 的发送端输出。
2. 配置丢包率后，包含连续相同 `num_acked` 值及 `got same ack <序列号>` 的发送端输出。
3. 接收端启动和通信成功的终端输出。

## 6. 结果说明

超时信号适用于长期未收到 ACK 的情况，重复 ACK 则可在后续乱序报文持续到达时更快暴露缺失的前序报文。两种机制均只提供丢包检测依据，后续重传策略不在本任务范围内。
