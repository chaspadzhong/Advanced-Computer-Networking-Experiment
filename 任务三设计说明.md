# 开发任务三设计说明

## 1. 任务目标

实现任务三指南要求的两种丢包检测信号：发送端超时检测，以及接收端产生、发送端检测重复 ACK。本任务只负责检测丢包，不实现丢包后的重传和 RACK。

## 2. 超时检测与窗口调整

发送端的 `sender.cc` 已在轮询超时时调用 `send_datagram( true )`。该调用把 `after_timeout=true` 传给 `Controller::datagram_was_sent()`，控制器据此判断发生超时。

控制器用 `window_size_` 保存窗口大小，初值为 50。发生超时时，若窗口大于 1 则将其减半，最小保持为 1；同时严格按指南示例输出：

```text
time out
```

`window_size()` 返回更新后的窗口值，使后续发送受到缩小窗口的限制。

## 3. 重复 ACK 实现

任务三以每收到一个数据报文就反馈一个 ACK 为前提。因此本分支不沿用任务二的延迟 ACK 计数器；任务二代码仍保留在 `ex2` 分支。

接收端在 `receiver.cc` 维护 `should_ack_num`，表示下一个应该按序到达的数据报文序列号：

1. 若当前报文序列号等于 `should_ack_num`，说明它按序到达，随后递增 `should_ack_num`。
2. 若不相等，说明当前报文之前仍有空缺。接收端把报文的序列号替换为 `should_ack_num - 1`，即最后一个连续按序收到的序列号。
3. 接收端调用已有的 `transform_into_ack()`。该函数把替换后的序列号写入 `ack_sequence_number`，因此后续乱序报文会得到相同确认号，形成重复 ACK。

发送端在 `Controller::ack_received()` 中保存最近一次确认号。若当前确认号与最近确认号相同，严格按指南示例输出：

```text
got same ack:<序列号>
```

## 4. 涉及文件

- `datagrump/controller.hh`：保存可变窗口大小和最近确认号。
- `datagrump/controller.cc`：超时缩窗与重复 ACK 检测。
- `datagrump/receiver.cc`：维护 `should_ack_num` 并生成重复 ACK。

## 5. 运行与截图记录（待补充）

本次提交只完成代码和说明文档，未执行运行测试。后续可按照任务三操作指南补充以下截图：

1. 不设置丢包率时，终端出现 `time out` 的输出。
2. 按指南在 `run-contest` 中配置 `mm-loss uplink 0.01` 后，终端连续出现 `got same ack:<序列号>` 的输出。

## 6. 结果说明

超时检测能在长期没有 ACK 时缩小窗口；重复 ACK 能在后续报文越过丢失报文时报告最后一个连续按序序号。两种信号共同用于发现可能丢失的数据报文，但不处理重传。
