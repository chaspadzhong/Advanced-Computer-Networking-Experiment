# 开发任务二设计说明

## 1. 任务目标

本任务在 `cc-training/datagrump` 中实现延迟 ACK 的“按数量触发”部分：接收端每收到两个数据报文，才发送一个确认报文（ACK）。同时，在发送端的拥塞控制器中输出 ACK 所确认的数据报文序列号，以便验证 ACK 的反馈规律。

本次实现不包含“等待超时后补发 ACK”的扩展机制，符合任务指南仅要求按指定数量触发 ACK 的范围。

## 2. 机制原理

普通 ACK 机制中，接收端每收到一个数据报文便立即反馈一个 ACK，数据报文与 ACK 的数量比为 1:1。ACK 本身通常不携带有效载荷，但仍带有固定长度的协议头，因此频繁发送 ACK 会增加协议开销。

延迟 ACK 通过合并多个数据报文的确认操作降低 ACK 数量。本实验将触发阈值设置为 2：接收端连续接收两个数据报文后，对第二个报文生成 ACK。若发送端的报文序列号从 0 开始，则正常无丢包、无乱序条件下，发送端将依次观察到：

```text
num_acked:1
num_acked:3
num_acked:5
...
```

因此，数据报文与 ACK 的数量比由 1:1 降为约 2:1。

## 3. 实现过程

### 3.1 接收端延迟 ACK

在 `datagrump/receiver.cc` 中增加以下状态：

- `ack_interval`：ACK 触发阈值，固定为 2。
- `packets_since_last_ack`：自上一次发送 ACK 后已接收的数据报文数。

接收端每收到一个数据报文便递增计数器。当计数值小于 2 时，接收端不发送 ACK，并继续接收下一份报文；当计数达到 2 时，接收端使用当前（第二个）数据报文调用 `transform_into_ack()`，发送 ACK 后把计数器清零。

由于 ACK 使用当前报文生成，其 `ack_sequence_number` 对应这一组中序列号较大的第二个数据报文。因此确认序列号为 1、3、5……，可以直观地体现每两个数据报文才产生一次确认。

### 3.2 发送端序列号打印

在 `datagrump/controller.cc` 的 `Controller::ack_received()` 中增加：

```cpp
cout << "num_acked:" << sequence_number_acked << endl;
```

`sequence_number_acked` 是当前 ACK 确认的数据报文序列号。该输出由发送端在收到 ACK 时产生，可用于观察延迟 ACK 的实际确认序列。

## 4. 涉及文件

- `datagrump/receiver.cc`：实现按两个数据报文触发一次 ACK 的计数逻辑。
- `datagrump/controller.cc`：输出 ACK 所确认的数据报文序列号。

## 5. 运行与截图记录（待补充）

本次提交只完成代码和说明文档，未执行运行测试。后续可在实验环境中启动接收端与发送端，并将以下截图添加到本节：

1. 编译成功的终端输出。
2. 接收端开始监听端口的输出。
3. 发送端的 `num_acked` 输出，其中连续确认序列号应为 `1、3、5……`，证明每收到两个数据报文才发送一个 ACK。

## 6. 结果说明

该实现减少了 ACK 发送频率，降低 ACK 协议头带来的额外开销。其限制是：若报文总数为奇数，最后一个未凑满两个的报文不会立即获得 ACK；任务指南所述的超时补发 ACK 可解决该情况，但不属于本任务要求的实现范围。
