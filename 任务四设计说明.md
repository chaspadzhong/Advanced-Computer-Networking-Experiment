# 开发任务四设计说明

## 1. 任务目标

本任务在 `ex4` 分支的控制器中完成 RTT/RTO 估计和 AIMD 拥塞控制。控制器根据 ACK 中携带的发送端时间戳计算往返时延，根据网络反馈动态返回重传超时时间，并在正常 ACK、重复 ACK 和超时三种事件下调整发送窗口。

## 2. RTT 与 RTO 计算

ACK 到达发送端的时间 `timestamp_ack_received` 与被确认数据报文的发送时间 `send_timestamp_acked` 都基于发送端时钟，因此：

```text
SampleRTT = timestamp_ack_received - send_timestamp_acked
```

第一次有效采样时：

```text
EstimatedRTT = SampleRTT
DevRTT = SampleRTT / 2
```

后续采样按任务指南的平滑公式更新：

```text
EstimatedRTT = 0.875 * EstimatedRTT + 0.125 * SampleRTT
DevRTT = 0.75 * DevRTT + 0.25 * abs(SampleRTT - EstimatedRTT)
RTO = EstimatedRTT + 4 * DevRTT
```

`timeout_ms()` 在尚未取得样本时返回默认值 1000 ms；取得样本后返回向上取整的 RTO，且最低为 1 ms。重复 ACK 对应旧的确认信息，不参与 RTT 估计，避免错误拉高采样值。

## 3. AIMD 算法

控制器维护以下状态：

- `window_size_`：拥塞窗口 `cwnd`，初始为 1。
- `ssthresh_`：慢启动阈值，初始为 16。
- `congestion_avoidance_ack_count_`：拥塞避免阶段累计的新 ACK 数量。

### 3.1 正常 ACK

当 `cwnd < ssthresh` 时处于慢启动阶段，每收到一个新的 ACK，窗口加 1；在一个 RTT 内会收到约一个窗口数量的 ACK，因此窗口呈指数趋势增长。

当 `cwnd >= ssthresh` 时进入拥塞避免阶段。每累计约 `cwnd` 个新的 ACK，窗口才增加 1，从而实现线性增长。

### 3.2 拥塞信号

超时或重复 ACK 都调用同一乘法减少过程：

```text
ssthresh = max(cwnd / 2, 1)
cwnd = 1
```

同时清零拥塞避免计数器。超时仍输出 `time out`；重复 ACK 仍输出 `got same ack:<序列号>`，与任务三验证格式一致。

## 4. 涉及文件

- `datagrump/controller.hh`：增加 RTT、RTO、慢启动阈值及窗口状态成员。
- `datagrump/controller.cc`：实现 RTT 平滑、RTO 返回、慢启动、拥塞避免和乘法减少。

## 5. 运行与截图记录（待补充）

本次提交只完成代码和说明文档，未执行运行测试。后续可按任务四指南配置有限队列和尾丢包模式，并补充：

1. `time out` 输出，证明超时和乘法减少路径被触发。
2. `got same ack:<序列号>` 输出，证明重复 ACK 拥塞信号被触发。
3. `mm-throughput-graph` 生成的吞吐量图，用于展示窗口增长和拥塞后的变化趋势。

## 6. 结果说明

RTO 使用平滑 RTT 与时延偏差共同估计，因此能随网络时延及波动变化。AIMD 在正常反馈下逐步探测可用带宽，在超时或重复 ACK 指示拥塞时快速降低发送窗口，以减轻拥塞并重新开始探测。
