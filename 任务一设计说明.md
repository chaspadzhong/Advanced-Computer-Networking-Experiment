# 开发任务一设计说明

## 1. 任务目标

为 `cc-training/datagrump` 中的通信报文增加“报文类型”元数据，使接收方能够无歧义地区分数据报文（DATA）和确认报文（ACK）。本次修改同时覆盖报文头的数据结构、字节流序列化、字节流解析、ACK 转换逻辑以及接收端日志输出。

## 2. 报文格式设计

原报文头含有六个 `uint64_t` 字段。现增加第七个字段 `message_type`，其底层类型为 `uint64_t`，占用 8 字节，并采用网络字节序传输。

| 字段 | 含义 |
| --- | --- |
| `sequence_number` | 当前报文的序列号 |
| `send_timestamp` | 当前报文的发送时间戳 |
| `ack_sequence_number` | 被确认的数据报文序列号 |
| `ack_send_timestamp` | 被确认数据报文的发送时间戳 |
| `ack_recv_timestamp` | 接收端收到被确认数据报文的时间戳 |
| `ack_payload_length` | 被确认数据报文的有效载荷长度 |
| `message_type` | 报文类型：`DATA = 0`，`ACK = 1` |

`MessageType` 使用枚举类型表达，避免在业务逻辑中直接使用无语义的数值。解析报文头时会校验该字段；若值不是 `0` 或 `1`，程序会抛出异常，避免继续处理格式错误或不兼容的报文。

## 3. 实施过程与机制说明

1. 在 `contest_message.hh` 的 `ContestMessage` 中定义 `MessageType` 枚举，并将 `message_type` 添加到 `Header`。
2. 新建数据报文时，`Header` 构造函数把类型初始化为 `DATA`。
3. `Header::to_string()` 将 `message_type` 转换为 `uint64_t`，再使用已有的 `put_header_field()` 以网络字节序追加到报文头末尾。
4. `Header(const string &)` 从第七个 64 位字段解析报文类型，并拒绝未知类型，保证双方对报文格式的解释一致。
5. 接收端调用 `transform_into_ack()` 后，原数据报文保留其确认相关元数据、清空载荷，并把 `message_type` 改为 `ACK`。因此 ACK 的类型由显式字段决定，而不再依赖确认序列号是否为默认值的隐式判断。
6. `is_ack()` 直接检查 `message_type`；接收端在转换 ACK 前输出接收报文的类型、序列号、发送时间戳和载荷长度，便于观察协议行为。

## 4. 关键代码位置

- `datagrump/contest_message.hh`：报文类型枚举与报文头字段定义。
- `datagrump/contest_message.cc`：报文头序列化、解析、合法性校验、ACK 类型转换与类型判断。
- `datagrump/receiver.cc`：接收数据报文后打印新增的类型信息。

## 5. 运行与截图记录（待补充）

本次提交按实验要求只完成代码和说明文档，未执行运行测试。运行环境准备完成后，可启动接收端与发送端，并将终端输出截图插入本节。

建议截图内容：

1. 接收端启动并监听指定端口的终端输出。
2. 接收端收到报文后的日志，日志中应包含 `Received message: message_type=DATA`、序列号、时间戳和载荷长度。
3. 若自行增加 ACK 日志或抓包，可补充显示 `ACK` 类型字段的截图，用于证明数据报文和确认报文已能被显式区分。

## 6. 结果说明

修改后，数据报文与 ACK 在同一 `ContestMessage` 格式下仍可复用公共字段，但通过 `message_type` 获得了明确的语义边界。报文头长度相应增加 8 字节；这会略微减少单个数据报文可用的有效载荷空间，但能消除两类报文的格式歧义，并为后续扩展控制报文等类型保留清晰的协议演进方式。
